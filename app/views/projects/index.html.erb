<div class="container my-5">
  <h1 class="text-center mb-5">💻 Mes projets</h1>
  <div class="row g-4">
    <% @projects.each do |project| %>
      <div class="col-md-6 col-lg-4">
        <div class="project-card shadow-sm h-100 d-flex flex-column">
          <% if project.image_url.present? %>
            <img src="<%= project.image_url %>" class="project-img" alt="<%= project.title %>">
          <% else %>
            <div class="project-placeholder d-flex align-items-center justify-content-center">
              <i class="fa-solid fa-code fa-2x text-muted"></i>
            </div>
          <% end %>
          <div class="p-3 d-flex flex-column flex-grow-1">
            <h5 class="fw-bold mb-2"><%= project.title %></h5>
            <p class="text-muted small flex-grow-1"><%= project.description.truncate(100) %></p>
            <div class="mt-3">
              <%= link_to "Voir détails", project_path(project), class: "btn-custom me-2" %>
              <% if project.github_url.present? %>
                <a href="<%= project.github_url %>" target="_blank" class="btn-custom-outline">
                  <i class="fa-brands fa-github"></i>
                </a>
              <% end %>
            </div>
          </div>
        </div>
      </div>
    <% end %>
  </div>
  <div class="text-center mt-5">
    <%= link_to "➕ Ajouter un projet", new_project_path, class: "btn btn-primary" %>
  </div>
</div>
